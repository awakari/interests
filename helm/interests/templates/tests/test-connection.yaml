apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "interests.fullname" . }}-test-connection"
  labels:
    {{- include "interests.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "interests.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
