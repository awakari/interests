subscriptions:
  - name: "sub0"
    description: "my subscription 0"
    routes:
      - route0
      - route1
    rule:
      type: group
      not: false # root rule can not be an exclusion
      logic: and # and/or/xor
      rules:
        - type: metadata_pattern
          not: false
          partial: false
          key: location
          pattern:
            code: ""
            src: ""
