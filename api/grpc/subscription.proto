syntax = "proto3";

package grpc;

option go_package = "./api/grpc";

import "api/grpc/matcher_group.proto";

message Subscription {
  string name = 1;
  string description = 2;
  repeated string routes = 3;
  MatcherGroup excludes = 4;
  MatcherGroup includes = 5;
}
